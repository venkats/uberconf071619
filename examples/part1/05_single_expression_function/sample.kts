fun greet() = when {
  else -> "Hello, this is stupid"
}

println(greet())

//a stupid example of a single expression function

//we don't have to specify the return type for 
//single expression functions