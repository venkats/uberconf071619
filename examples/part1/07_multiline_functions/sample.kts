fun greet(): String {
  val message = "Hello there"
  return message
}

println(greet())

//multi-line (block) functions
//return type is required or considere to be Unit (void)
//otherwise

//return keyword is required for anything other than Unit